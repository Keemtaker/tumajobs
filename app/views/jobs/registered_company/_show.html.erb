<br>
<br>
<div class="container">
  <div class="card bg-transparent" style="border: none !important;">
    <div class="card-body">
      <div class="row">
        <div class="col-sm-2" style="margin-bottom: 30px;">
          <% if @job.company.logo?  %>
            <%= image_tag @job.company.logo_url, class: "show-logo" %>
          <% else %>
            <%= image_tag "fallback.png", class: "show-logo" %>
          <% end %>
        </div>
        <div class="col-sm-8">
          <h1><%= @job.title %></h1>
          <span class="job-info">
            <h3 style="color: #5DB1D1;"><%= @job.company.name %></h3>
            <h3><%= @job.salary %></h3>
          </span>
        </div>
      </div>
    </div>
  </div>
  <br>

  <div class="col-md-10">
    <% if @job.job_email? %>
      <%= link_to "Quick Apply", new_job_talent_path(@job), class: "btn btn-success btn-lg" %>
    <% else %>
      <%= link_to "Apply", @job.job_url, :target => "_blank", class: "btn btn-success btn-lg fas fa-external-link-alt" %>
    <% end %>
  </div>
  <br>

  <br>
  <div class="col-md-12">
    <span><i class="fas fa-globe" aria-hidden="true"></i> <%= @job.location %></span>
    <span style="margin-left: 40px; color: #5DB1D1;"><i class="far fa-clock" aria-hidden="true"></i> <%= @job.job_type %></span>
  </div>
  <% if policy(@job).edit? %>
    <div class="col-md-3", style="margin-top: 40px;">
      <%= link_to "Edit job details", edit_company_job_path(@job.company, @job), class: "btn btn-md btn-block btn-outline-primary"%>
    </div>
    <div class="col-md-3", style="margin-top: 40px;">
      <%= link_to "Delete Job", company_job_path(@job.company, @job), method: :delete, data: { confirm: "Are you sure you want to delete the job posting?" },
      class: "btn btn-md btn-block btn-outline-danger"%>
    </div>
  <% end %>

  <%= render "jobs/registered_company/shared_forms/show_create" %>

</div>
